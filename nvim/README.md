# nvim config

Requires nvim ≥ 0.12, ripgrep, a C compiler, tree-sitter CLI (`bin/setup` checks).

```bash
bin/setup    # after git pull / fresh machine — sync plugins to lazy-lock.json
bin/update   # update plugins + parsers, then commit lazy-lock.json
bin/health   # :checkhealth
```
